/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i2-prod.liverpoolecho.co.uk',
      'sp-ao.shortpixel.ai',
      'upload.wikimedia.org',
    ],
  },
};

module.exports = nextConfig;
